module app;

import client;
import gcalendar.calendar;
import std.stdio;

int main() {
    Client client = new Client("secret.json");

    Calendar calendar = new Calendar(client);

    auto res = calendar.acl().list("dapilic2@gmail.com").execute();

    writeln(res.toString);

    return 0;
}